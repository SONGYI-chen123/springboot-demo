server:
  port: 9090

spring:
  datasource:
    url: jdbc:mysql://localhost/tacocloud
    username: tacodb
    password: tacopassword

logging:
  level:
    root: WARN
    org.springframework.security: DEBUG
